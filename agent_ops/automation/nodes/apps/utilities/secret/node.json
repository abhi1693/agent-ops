{
  "node": "tool.secret",
  "details": "Resolve a scoped secret and store the redacted value path in context.",
  "agentOps": {
    "kind": "tool",
    "displayName": "Resolve secret",
    "icon": "mdi-key-variant",
    "appId": "utilities",
    "appLabel": "AgentOps utilities",
    "appDescription": "AgentOps-specific helper nodes that are intentionally separate from the n8n-style built-in set.",
    "appIcon": "mdi-tools",
    "config": {
      "output_key": "credentials.value"
    },
    "fields": [
      {
        "key": "output_key",
        "label": "Save result as",
        "type": "text",
        "placeholder": "credentials.openai"
      },
      {
        "key": "secret_name",
        "label": "Secret name",
        "type": "text",
        "placeholder": "OPENAI_API_KEY"
      },
      {
        "key": "secret_group_id",
        "label": "Secret group",
        "type": "text",
        "placeholder": "Use workflow secret group",
        "help_text": "Optional. Override the workflow secret group for this node with a scoped secret group ID."
      }
    ]
  }
}
