{
  "node": "trigger.kibana_webhook",
  "details": "Kibana webhook trigger for observability rule ingestion.",
  "agentOps": {
    "kind": "trigger",
    "displayName": "Kibana webhook",
    "icon": "mdi-view-dashboard-outline",
    "appId": "observability",
    "appLabel": "Observability",
    "appDescription": "Ingest alerts and query monitoring systems such as Prometheus and Elasticsearch.",
    "appIcon": "mdi-chart-areaspline",
    "config": {},
    "fields": [
      {
        "key": "secret_name",
        "label": "Secret name",
        "type": "text",
        "placeholder": "KIBANA_WEBHOOK_SECRET"
      },
      {
        "key": "secret_group_id",
        "label": "Secret group",
        "type": "text",
        "placeholder": "Use workflow secret group",
        "help_text": "Optional. Override the workflow secret group for this trigger with a scoped secret group ID."
      },
      {
        "key": "secret_header",
        "label": "Secret header",
        "type": "text",
        "placeholder": "X-AgentOps-Webhook-Secret"
      }
    ]
  }
}
