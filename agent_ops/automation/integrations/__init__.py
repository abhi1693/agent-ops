"""
Workflow integration packages.

Each integration directory maps to one external system.
Taxonomy groupings such as "observability" are metadata only and must not be
used as package boundaries.
"""
